# Repository workspace adequacy capability

Provides the exact nine-slot bounded listing and UTF-8 reads, literal search, fixed `bun test` process, and four request-bound atomic replacements inside one receiver-owned controlled repository root. Live replacements may be admitted by either an exact interactive confirmation or a request-and-proposal-bound `router-policy-proposal-v1` receiver verdict.
