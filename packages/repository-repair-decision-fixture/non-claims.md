# Non-claims

This fixture is not a model, runtime strategy, or general repository repair policy. It is bound to the exact two-identity compatibility set in `application-ids.json` and one conformance sequence.
