# Repository workspace actuality capability

Provides bounded listing, UTF-8 reads, literal search, the fixed `bun test` process, and request-bound atomic replacement inside one receiver-owned temporary repository root.
