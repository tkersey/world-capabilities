# Repository repair OpenAI decision capability

Resolves the exact `model.decide.v1` request through the OpenAI Responses API using receiver-owned credentials and a strict, checksummed Action schema. No provider tool is enabled.
