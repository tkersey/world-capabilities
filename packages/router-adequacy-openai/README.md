# Router adequacy OpenAI decision capability

Resolves the exact router-policy `model.decide.v1` request through the OpenAI Responses API using receiver-owned credentials and the checksummed DecisionContract's strict Action schema. No provider tool is enabled.
